Run the "full_data_analysis" script in the parent folder on the Estrela dataset (uncomment the line that selects the Estrela dataset),
to produce the panel in Fig.3B.

Individual eigenvectors in panel Fig.3C can be produced running the "plot_eigenvector" script in the parent directory, 
setting 'dataset_name=Estrella_full'.

The analysis of the batch-culture simulated dataset is done separately in its own script, in "simulated_datasets/CR_batch"

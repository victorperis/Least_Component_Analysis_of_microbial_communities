run the data analysis section of the script to get the panels in SI Fig.17.
Make sure to copy-paste the actual datasets in this directory before running the script, and selecting the right dataset
in the script.

'highvar' --> for panel A.
'lowvar' ---> for panel B.
for panel C, use the data from 'lowvar", but set "relative_abundances_bool=True" in the script. This takes the dataset and rescales it to relative abundances. 

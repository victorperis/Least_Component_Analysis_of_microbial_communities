run "plot_IPR_boxplots_forall_Grilli_datasets" script to produce panel Fig.5A.

To produce panel Fig.5B, run the "plot_eigenvectors" script in the parent directory. 
Make sure to select the right dataset inside the script ('Grilli.Soil') and mode (modes 0 and 2 for LC1 and LC3, respecitvely). 
Outputs will be in "individual_eigenvectors/"

To produce panels Fig.5C and D, run the "full_data_analysis" script in the parent directory. 
Make sure to select the right dataset inside the script ('Grilli.Soil'). Outputs will be in "out/Grilli.Soil/figures".
